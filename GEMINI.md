# GEMINI.md

Follow the CareerFit AI project harness.

Primary files:
- `harness/MAIN_HARNESS.md`
- `harness/ROUTING.md`

Use only the relevant files selected by `harness/ROUTING.md`.

Hard reminders:
- Work only inside `frontend/` for React UI tasks.
- Do not modify `backend/` unless explicitly requested.
- Never put API keys in React code.
- Never expose `.env` values.
- Do not change the `/analyze` response schema.
- Prefer one file, one task, one change.

For UI design:
- Follow `harness/skills/design-skill.md`.

For security and tests:
- Follow only the relevant files in `harness/checks/`.